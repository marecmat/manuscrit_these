% \begin{tikzpicture}
%     \def\thstart{20} \def\thend{70} \def\rin{.5} \def\rout{.7} \def\pad{.1}
%     \def\dd{2}\def\hh{2}
%     \draw[thick] (0, 0) rectangle (\dd, \hh);
%     \draw (\dd/2, \hh/2) ++(\thstart+360:\rout) arc (\thstart+360:\thend:\rout);
%     \draw (\dd/2, \hh/2) ++(\thstart+360:\rin) arc (\thstart+360:\thend:\rin);
%     \draw (\dd/2, \hh/2) ++(\thstart+360:\rout) -- ++(\thstart+360:-\rout+\rin);
%     \draw (\dd/2, \hh/2) ++(\thend:\rout) -- ++(\thend:-\rout+\rin);
    
%     \draw[->] (\dd/2, \hh/2) -- ++(\thend+70:\rin) node[above] {$r_{in}$};
%     \draw[->] (\dd/2, \hh/2) -- ++(\thend+110:\rout) node[above] {$r_{out}$};
%     \draw[<->] (\dd/2, \hh/2) ++(\thstart:\rout + 2*\pad) arc[start angle=\thstart, end angle=\thend, radius=\rout + 2*\pad] node[midway, right] {$\alpha_s$};
%     \pgfmathsetmacro{\thmid}{(\thend+\thstart)/2}
%     \draw[dashed] (\dd/2, \hh/2) -- ++(\thmid/2:\rout + 10*\pad);
%     \draw[dashed] (\dd/2, \hh/2) -- ++(0:\rout + 10*\pad);
% \end{tikzpicture}    
\begin{tikzpicture}[scale=3]
    \def\thstart{15} \def\thend{60} \def\rin{.4} \def\rout{.6} \def\pad{.1}
    \def\dd{2}\def\hh{2}
    \coordinate (O) at (\dd/2, \hh/2);
    \pgfmathsetmacro{\thmid}{(\thstart+\thend)/2}

    % \draw[thick] (0, 0) rectangle (\dd, \hh);
    \draw (O) ++(\thstart+360:\rout) arc (\thstart+360:\thend:\rout);
    \draw (O) ++(\thstart+360:\rin) arc (\thstart+360:\thend:\rin);
    \draw (O) ++(\thstart+360:\rout) -- ++(\thstart+360:-\rout+\rin);
    \draw (O) ++(\thend:\rout) -- ++(\thend:-\rout+\rin);
    \draw[->] (O) -- ++(\thend+70:\rin) node[above] {$r_{in}$};
    \draw[->] (O) -- ++(\thend+110:\rout) node[above left] {$r_{out}$};
    \draw[<->] (O) ++(\thstart:\rout + \pad) arc[start angle=\thstart, end angle=\thend, radius=\rout + \pad] node[midway, above] {$\alpha_s$};
    \draw[dashed] (O) -- ++(\thmid:\rout + 3*\pad);
    \draw[dashed] (O) -- ++(0:\rout + 3*\pad);
    \draw[<->] (O) ++(0:\rout + 3*\pad) arc[start angle=0, end angle=\thmid, radius=\rout + 3*\pad] node[midway, right] {$\alpha_0$};
\end{tikzpicture}