\begin{tikzpicture}
    \def\cellen{2} \def\height{2} \def\pad{.3} \def\arrow{.7}\def\totalen{6}
    % Shadings 
    \draw[top color = white, bottom color=fluid, draw=none] (-2*\pad, \height) rectangle ++ (\totalen+4*\pad, 4*\pad);
    \draw[top color = fluid, bottom color = white,draw=none] (-2*\pad, 0) rectangle ++ (\totalen+4*\pad, -4*\pad);
    \draw[fill=porous, draw=none] (-2*\pad, 0) rectangle ++ (\totalen+4*\pad, \height);

    \draw[black] (-2*\pad, 0) -- ++ (\totalen+4*\pad, 0);
    \draw[black] (-2*\pad, \height) -- ++ (\totalen+4*\pad, 0);
    \foreach \i in {0, 1, 2} {
        % \draw[fill=white] ((\i*\cellen + \cellen/2, \height/2) circle (2*\pad);
        \draw[fill=white] plot[smooth cycle] coordinates {
            (\i*\cellen + \cellen/2, \height/2-2*\pad) 
            (\i*\cellen + \cellen/2 - 2*\pad, \height/2-\pad)
            % (\i*\cellen + \cellen/2-1.3*\pad, \height/2)
            (\i*\cellen + \cellen/2, \height/2+2*\pad)
            (\i*\cellen + \cellen/2 + 2*\pad, \height/2)
        };
        \draw[dashed] (\i*\cellen, 0) -- ++ (0, \height);
        \draw[dashed] (\i*\cellen+\cellen, 0) -- ++ (0, \height);
        }

    \draw[thick, ->] (1.2*\cellen, \height) -- ++ (25:\arrow);
    \draw[thick, ->] (1.2*\cellen, \height) -- ++ (50:\arrow);
    \draw[thick, ->] (1.2*\cellen, \height) -- ++ (75:\arrow);
    \draw[thick, ->] (1.5*\cellen, 0) -- ++ (180-25:-\arrow);
    \draw[thick, ->] (1.5*\cellen, 0) -- ++ (180-50:-\arrow);
    \draw[thick, ->] (1.5*\cellen, 0) -- ++ (180-75:-\arrow);

    \draw[thick,->] (\cellen, 0) -- ++ (\arrow, 0) node[above] {$x_1$};
    \draw[thick, ->] (\cellen, 0) -- ++ (0, \arrow) node[below right] {$x_2$};
    \draw[<->] (-.8*\pad, 0) -- ++ (0, \height) node[midway, left] {$h$};
    \draw[<->] (0, -.8*\pad) -- ++ (\cellen, 0) node[midway, below] {$d$};
    \node at (2*\cellen-\pad, 4*\height/5) {$\Omega$};

    \node at (\totalen-\cellen/2, -\pad) (b) {$\Gamma_b$};
    \node at (\totalen-\cellen/2, \height+\pad) (t) {$\Gamma_t$};
    \node at (\totalen-\cellen+\pad, \pad) (l) {$\Gamma_l$};
    \node at (\totalen-\pad, \height-\pad) (r) {$\Gamma_r$};

    \node at (1.2*\cellen + 2.5*\pad, \height+\arrow) {$a_q^t$};
    \node at (1.7*\cellen + 1*\pad, -\arrow) {$a_q^b$};
\end{tikzpicture}    
